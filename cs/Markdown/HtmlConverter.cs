using System.Text;

namespace Markdown;

public static class HtmlConverter
{
    public static string ConvertToHtml(IToken[] tokens)
    {
        var stringBuilder = new StringBuilder();
        
        return stringBuilder.ToString();
    }
}